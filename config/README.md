# SentraAI Configuration

Centralized settings, environmental variables, and default configuration profiles.

## Layout

* `settings.py`: Pydantic BaseSettings class loading configuration from environment variables.
* `profiles/`: Ingress and scanner profiles specifying custom severity mappings or rule configurations.
